/** Stub — dashboard do cliente (progresso das 7 etapas). Implementado pelo agente de página. */
export default function AppDashboard() {
  return (
    <div>
      <h1 className="font-display text-3xl uppercase">Painel</h1>
      <p className="mt-2 text-text-muted">Acompanhamento das 7 etapas do seu processo. Em construção.</p>
    </div>
  );
}
