/** Stub — visão geral admin (KPIs, funil, gráfico de leads). Implementado pelo agente de página. */
export default function AdminOverview() {
  return (
    <div>
      <h1 className="font-display text-3xl uppercase">Visão geral</h1>
      <p className="mt-2 text-text-muted">KPIs, funil de conversão e gráfico de leads. Em construção.</p>
    </div>
  );
}
